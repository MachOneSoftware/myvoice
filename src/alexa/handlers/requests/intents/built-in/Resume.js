/**
 * Common Utterances: 
 *  • resume
 *  • continue
 *  • keep going
 * 
 * Purpose:
 *  Lets the user resume or continue an action.
 *  You must implement this intent if your skill streams audio using the AudioPlayer interface. 
 *  Users can invoke this intent without using your invocation name if your skill is currently 
 *  playing audio or was the most recent skill to play audio.
 */

module.exports = {
    handle(handlerInput) {
        // Resume
    }
};